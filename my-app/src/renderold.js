/*
if (currentTagsApplied === 1 && !unfilter && li[i].getAttribute("data-active") === "true"){
	li[i].style.display = "none";
	li[i].setAttribute("data-showing","false");
}
//show only those that are active and fit the filter
if(!unfilter){
	if(intersection.length > 0 && li[i].getAttribute("data-active") === "true"){
		li[i].style.display = "";
		li[i].setAttribute("data-showing","true");
	}
}
*/

/*
else{
	//hide all active that fit the filter
	if(intersection.length > 0 && li[i].getAttribute("data-active") === "true"){
		li[i].style.display = "none"; //hide it
		li[i].setAttribute("data-showing","false");
	}
}
//if removing only active filter
//show all active by default
if(currentTagsApplied === 0 && unfilter){
	if(li[i].getAttribute("data-active") === "true"){
		li[i].style.display = "";
		li[i].setAttribute("data-showing","true");
	}
}*/
